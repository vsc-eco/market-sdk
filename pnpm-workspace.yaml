packages:
  - 'packages/*'
  - 'examples/*'

# pnpm 11 approval for dependency build/install scripts.
# esbuild fetches its platform binary; secp256k1 compiles a native addon;
# core-js runs a (harmless) postinstall notice.
allowBuilds:
  core-js: true
  esbuild: true
  secp256k1: true
